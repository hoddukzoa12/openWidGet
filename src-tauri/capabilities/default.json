{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the OpenWidGet shell preview window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
